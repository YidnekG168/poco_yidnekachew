
/* const userInput = prompt("Enter a string:");
if (userInput !== null) {
   
    const stringLength = userInput.length;
  
    
    alert(`You entered: "${userInput}"\nIt contains ${stringLength} characters.`);
  } else {
    
    alert("You didn't enter a string.");
  } */

  const userInput = prompt("Enter a string:");


const stringLength = userInput.length;


console.log(`You entered: "${userInput}"\nIt contains ${stringLength} characters.`);
